name: Terraform Plan

on:
    push:
        branches:
            - master

jobs:
    terraform:
        runs-on: ubuntu-latest
    
        steps:
            - uses: actions/checkout

            - name: Setup Terraform
              uses: hashicorp/setup-terraform@v1